---
phase: 03-uses-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/uses.md
autonomous: true

must_haves:
  truths:
    - "User sees 'Uses' link in footer navigation"
    - "User clicks 'Uses' and views uses page"
  artifacts:
    - path: 'site/uses.md'
      provides: 'Uses page with footer navigation'
      contains: 'tags: foot'
  key_links:
    - from: 'site/uses.md'
      to: 'layouts/base.njk footer'
      via: 'tags: foot + eleventyNavigation'
      pattern: "tags:\\s*foot"
---

<objective>
Add uses page to site footer navigation.

Purpose: Users can discover and access the uses page from any page on the site.
Output: Uses page appears in footer navigation alongside Now link.
</objective>

<execution_context>
@/Users/mcravey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mcravey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-uses-navigation/03-CONTEXT.md

Reference implementation (Now page with footer nav):
@site/now/index.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add footer navigation to uses page</name>
  <files>site/uses.md</files>
  <action>
    Add to uses.md frontmatter:
    - `tags: foot` - includes page in footer navigation collection
    - `eleventyNavigation` with key "Uses" and order 1 (after Now which is order 0)

    The existing frontmatter has:
    ```yaml
    ---
    title: Uses
    layout: page
    ---
    ```

    Update to:
    ```yaml
    ---
    title: Uses
    layout: page
    tags: foot
    eleventyNavigation:
      key: Uses
      order: 1
    ---
    ```

    This follows the established pattern from site/now/index.md.

  </action>
  <verify>
    1. Build site: `npm run build` (or equivalent)
    2. Check footer nav includes Uses link: `grep -i "uses" _site/index.html | grep -i "footer\|nav"`
    3. Verify uses page exists: `ls _site/uses/index.html`
  </verify>
  <done>
    Uses link appears in footer navigation on all pages, after Now link.
    Clicking Uses link navigates to /uses/ page with current content.
  </done>
</task>

</tasks>

<verification>
1. Build completes without errors
2. Uses link appears in footer navigation
3. Uses page accessible at /uses/
4. Navigation order: Now (0), Uses (1)
</verification>

<success_criteria>

- User sees "Uses" link in site footer navigation from any page
- User clicks "Uses" link and views uses page with current tools/setup content
- Navigation link ordering is sensible (Now before Uses)
  </success_criteria>

<output>
After completion, create `.planning/phases/03-uses-navigation/03-01-SUMMARY.md`
</output>
