---
layout: base
css: home.css
---
<section class="hero">
  <div class="hero-text">
    <h1>Hey y'all<span class="accent">,</span><br>I'm Mike Cravey.</h1>
    <p>Father, husband, teacher, and geek. A UX Engineer who believes the web should be fast, accessible, and built with care.</p>
    <div class="hero-links">
      {%- for contact in metadata.author.contacts %}
      <a href="{{ contact.url }}">{{ contact.name }} &rarr;</a>
      {%- endfor %}
    </div>
  </div>
  <div class="hero-portrait">
    <img src="/img/portrait.jpg" alt="Mike Cravey">
  </div>
  {# Mobile Option C layout (shown via CSS) #}
  <div class="hero-top">
    <div class="hero-avatar">
      <img src="/img/portrait.jpg" alt="Mike Cravey">
    </div>
    <div>
      <div class="hero-name">Hey y'all<span class="accent">,</span><br>I'm Mike Cravey.</div>
      <div class="hero-tagline">UX Engineer &middot; Lake Jackson, TX</div>
    </div>
  </div>
</section>

{% include "partials/divider.njk" %}

<section class="section-grid about">
  <div class="section-label">About</div>
  <div class="about-content">
    <p>I'm a UX Engineer based in Lake Jackson, TX. I spend my days thinking about the intersection of design and engineering — building interfaces that are both beautiful and performant.</p>
    <p>You can see what I'm up to <a href="/now/">now</a>, read more about how this site is made on the <a href="/colophon/">colophon</a>, or see what tools I use on the <a href="/uses/">uses</a> page.</p>
  </div>
</section>

{% include "partials/divider.njk" %}

<section class="section-grid posts">
  <div class="section-label">Latest Posts</div>
  <div class="post-list">
    {%- set recentPosts = collections.post | reverse | head(3) %}
    {%- for post in recentPosts %}
    <div class="post-item">
      <div class="post-date">{{ post.date | prettyDate }}</div>
      <div>
        <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
        {%- if post.data.description %}
        <p class="post-excerpt">{{ post.data.description }}</p>
        {%- endif %}
        {%- if post.data.tags %}
        <div class="post-tags">
          {%- for tag in post.data.tags | contentTags %}
          <a href="/tags/{{ tag | slugify }}/" class="tag-chip">{{ tag }}</a>
          {%- endfor %}
        </div>
        {%- endif %}
      </div>
    </div>
    {%- endfor %}
    <a href="/posts/" class="view-all">View all posts &rarr;</a>
  </div>
</section>
