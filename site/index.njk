---
layout: base
css: home.css
---
<header>
	<h1>{{ '/img/logo-text.svg' | svgContents | safe }}</h1>

	<img class="headshot" src="/img/illustration.png" alt="Caricature of headshot of Mike Cravey." />

	<section class="hero">
		<p>
			<span class="welcome">Hey y’all<span class="accent">,</span></span>
			my name is
			<strong>Mike Cravey</strong>. I'm a father, husband, teacher, and geek.
		</p>
	</section>
</header>

<main data-theme="dracula-light">
	<section class="adaptable-columns">
		<div class="heading">
			{% set about = collections.about | last %}
			<h2 class="h3">{{ about.data.title }}</h2>

			{% include "contact-links.njk" %}
		</div>

		<div class="content">
			{{ about.content | safe }}
		</div>
	</section>

	<section class="adaptable-columns">
		<div class="heading">
			<h2 class="h3"><a href="/posts">Posts</a></h2>
		</div>
		<div class="content">
			{% set post = collections.post | last %}
			{% include "post-link.njk" %}
		</div>
	</section>
</main>
