{
	"_id": "_design/posts",
	"_rev": "7-06db245dc158a5f6a381ff2a11bd3bf2",
	"language": "javascript",
	"views": {
		"posts": {
			"map": "function(doc) {
				if (doc.type === 'post') {
					emit(doc._id, doc);
				}
			}"
		},
		"byDate": {
			"map": "function(doc) {
				if (doc.type === 'post') {
					var ts = new Date(doc.timestamp),
							dateArr = [ts.getFullYear(), ts.getMonth() + 1, ts.getDate(), ts.getHours(), ts.getMinutes()];
					emit(dateArr, doc);
				}
			}"
		}
	}
}
